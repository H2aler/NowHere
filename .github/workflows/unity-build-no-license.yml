name: Unity Build (No License Required)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        lfs: true
        
    - name: Cache Unity Library
      uses: actions/cache@v3
      with:
        path: Library
        key: Library-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
        restore-keys: |
          Library-
          
    - name: Setup Unity
      uses: game-ci/unity-setup@v2
      with:
        unity-version: 2023.3.0f1
        
    - name: Build Unity Project (Android) - No License
      uses: game-ci/unity-builder@v2
      with:
        targetPlatform: Android
        buildMethod: NowHere.Editor.SimpleGitHubBuild.BuildAndroidAPK
        # 라이선스 없이 빌드
        unityLicense: ""
        
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: Android Build (No License)
        path: build/Android
        
    - name: Upload APK to Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: build/Android/*.apk
        tag_name: v${{ github.run_number }}-no-license
        name: NowHere Build v${{ github.run_number }} (No License)
        body: |
          ## NowHere AR/VR/XR MMORPG Build (No License)
          
          ### 🎮 Build Information
          - **Version**: v${{ github.run_number }}
          - **Platform**: Android
          - **Build Date**: ${{ github.event.head_commit.timestamp }}
          - **Commit**: ${{ github.sha }}
          - **License**: Personal (Free)
          
          ### 📱 Installation
          1. Download the APK file
          2. Enable "Unknown Sources" in Android settings
          3. Install the APK
          
          ### 🎯 Features
          - ✅ Complete Mobile Game
          - ✅ AR/VR/XR Support
          - ✅ Multiplayer
          - ✅ RPG System
          - ✅ Combat System
          - ✅ Save/Load System
          - ✅ Analytics
          - ✅ Testing Tools
          
          ### 🚀 System Requirements
          - Android 7.0+ (API Level 24+)
          - ARCore support for AR features
          - OpenXR compatible device for VR features
          
          ### 📋 Permissions
          - Camera (AR features)
          - Microphone (Voice commands)
          - Location (GPS features)
          - Internet (Multiplayer)
          - Network State (Connection status)
          - Bluetooth (VR controllers)
          - Vibrate (Haptic feedback)
          - Storage (Save data)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
